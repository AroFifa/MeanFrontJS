[build]
  command = "npm config set @bryntum:registry=https://npm.bryntum.com && npm login --registry=https://npm.bryntum.com --username $NPM_USERNAME --password $NPM_PASSWORD && npm install"


[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
